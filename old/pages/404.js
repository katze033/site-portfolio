import React from "react"
import Layout from '../components/layout'

const RedirectPage = () => {
    return (
        <Layout>
            <div>
                <p>Sorry. The page you are looking for has either moved or never existed. Click the back button or any of the navigation links above to redirect yourself.</p>
            </div>
        </Layout>
    )
}

export default RedirectPage